export async function getPhotographers() {
    // TODO: fetch "./data/photographers.json", vérifier response.ok,
    // parser en JSON (typer le résultat en PhotographersResponse),
    // puis retourner result.photographers
}
